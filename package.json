{
  "name": "gearworks",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node scripts/prepare-assets.mjs && vite --host 127.0.0.1 --port 4173",
    "build": "node scripts/prepare-assets.mjs && vite build --base=./",
    "preview": "vite preview --host 0.0.0.0 --port 4173"
  },
  "dependencies": {
    "three": "0.179.1"
  },
  "devDependencies": {
    "vite": "6.4.3"
  }
}
